<form>
  Post: <input type="text" />
  <input type="submit" value="Post" onClick="postChatMessage()" />
</form>

<div id="chat">
  <% @chat_messages.each do |chat_message| %>
    <p><%= chat_message.content %></p>
  <% end %>
</div>
